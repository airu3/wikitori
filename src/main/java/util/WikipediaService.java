package util;

import java.util.List;

public class WikipediaService {
    private static void fetchWordsFromWikipedia(String searchTerm, List<String> words, List<String> links) {
        // Wikipedia API の呼び出しと単語の取得処理を実装
        // ...

        // 取得した単語をリストに追加
        // words.add(word);
        // links.add(link);
    }
}
